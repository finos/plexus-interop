{
  "private": true,
  "name": "@plexus-interop/web",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Plexus Web Interop root module",
  "scripts": {
    "prebootstrap": "node ./scripts/replace-lock-registry.js",
    "preinstall": "node ./scripts/replace-lock-registry.js",
    "bootstrap": "lerna bootstrap",
    "postinstall": "node ./scripts/replace-lock-registry.js --backward",
    "prebuild": "run-s bootstrap",
    "build": "lerna run build --stream --concurrency 1",
    "publish": "lerna exec -- npm publish",
    "e2e-native": "cd ./packages/e2e && yarn e2e-native",
    "e2e-web": "cd ./packages/e2e && yarn e2e-web-electron",
    "poste2e-native": "yarn coverage",
    "poste2e-web": "yarn coverage",
    "coverage": "nyc report --reporter=text --reporter=html",
    "build-win": "run-s build e2e-native e2e-web-electron",
    "ci-build": "run-s build",
    "preci-build": "run-s gen-all-npmrc",
    "postci-build": "run-s clean-npmrc",
    "ci-publish": "npm run build && lerna exec --stream --concurrency 1 -- npm publish --verbose",
    "preci-publish": "run-s gen-all-npmrc bump-version",
    "postci-publish": "run-s clean-npmrc",
    "bump-version": "cross-var lerna publish --repo-version $PackageVersion --skip-git --skip-npm --yes --verbose",
    "gen-root-npmrc": "node ./scripts/gen-npmrc.js",
    "gen-all-npmrc": "shx cp .ci-npmrc-tpl .npmrc && node ./scripts/replace-env-vars.js --input .npmrc && lerna exec -- cross-var shx cp $LERNA_ROOT_PATH/.npmrc .npmrc",
    "clean-npmrc": "shx rm -rf .npmrc && lerna exec -- shx rm -rf .npmrc",
    "benchmarks": "cd ./packages/e2e && yarn native-broker-benchmarks && yarn web-broker-benchmarks"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "cross-var": "1.1.0",
    "electron": "1.7.12",
    "lerna": "2.8.0",
    "minimist": "1.2.0",
    "npm-run-all": "4.1.1",
    "shx": "0.2.2",
    "yarn": "1.3.2"
  }
}
