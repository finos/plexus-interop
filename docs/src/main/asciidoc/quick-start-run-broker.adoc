First of all we need to run Interop Broker, which is responsible for communication between clients. You can start it with CMD file on Windows:
[source, bash]
----
bin/win-x86/samples/LaunchBroker.cmd
----